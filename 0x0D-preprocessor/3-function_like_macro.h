#ifndef FUNCTION_LIKE_MACRO_H
#define FUNCTION_LIKE_MACRO_H

/**
 * Description - fuction like macro ABS(x) that computes the abs value of x
 *@x: value to compute the absolite value of
 */

#define ABS(x) ((x) < 0 ? (-x) : (x))

#endif
