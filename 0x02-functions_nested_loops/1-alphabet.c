#include"main.h"
/**
 * print_alphabet - prints alphabet in lower case followed by an new line
 *
 */
void print_alphabet(void)
{
	char alpha;

	for (alpha = 'a'; alpha <= 'z'; letter++)
		_putchar(alpha);

	_putchar('\n');
}
